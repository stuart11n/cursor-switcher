# cursor-switcher
switch cursor between primary and secondary monitor by clicking back button on mouse _when Flightsimulator2024.exe_ is running

```
cl cursor-switcher.c /link user32.lib gdi32.lib /SUBSYSTEM:WINDOWS /ENTRY:mainCRTStartup
```
